<div class="row posts-wrapper text-center">
  <div class="post text-center">
    <div class="post-head">
      <div class="name">
        <!-- SELF CREATED instead of a simply string -->
        <!-- this simply displays the current_user and not the user to whom the post is associated. -->
        <!-- <%= current_user.user_name %> -->
        <!--  this will show the posts associated with the current user-->
        <%= @post.user.user_name %>
      </div>
    </div>
    <div class="image center-block">
      <!-- this displays the image and caption through the show method in the PostsController -->
      <%= image_tag(@post.image, size: '500x500') %>
    </div>
    <p class="caption"><%= @post.caption %></p>
    <div class="form-wrapper">
      <p> Choose another picture and or caption for this post </p>

      <%= render 'partials/form' %>

      <div class="text-center edit-links">
        <!-- this has a link called Delete post that links the current post to the destroy method that is inbuilt in the routes and the CRUD so the route will direct it to the delete action in the crud
      link_to is the link on the view with the "Delet Post" as the written link
       posts_path(@post) is the route
     method is the actual change in the database-->
        <p><%= link_to "Delete Post", post_path(@post), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %> | <%= link_to "cancel", posts_path %></p>
        <!--  this links back to the index-->
      </div>
    </div>
  </div>
</div>
